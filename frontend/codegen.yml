overwrite: true
schema: "http://localhost:4000"
# documents: "src/**/*.tsx"
generates:
  # src/generated/graphql.ts:
  #   plugins:
  #     - "typescript"
  #     - "typescript-operations"
  ./data/schema.graphql:
    plugins:
      - schema-ast
